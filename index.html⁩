<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Zane Gamez — Coming Soon</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    :root {
      --bg1: #0f0c29;
      --bg2: #302b63;
      --bg3: #24243e;
      --accent: #00ffcc;
      --text: #eaeaea;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", Arial, sans-serif;
    }

    body {
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(270deg, var(--bg1), var(--bg2), var(--bg3));
      background-size: 600% 600%;
      animation: gradientMove 12s ease infinite;
      color: var(--text);
      text-align: center;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .card {
      background: rgba(0, 0, 0, 0.55);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 16px;
      padding: 40px 28px;
      max-width: 480px;
      width: 90%;
      backdrop-filter: blur(8px);
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.6);
    }

    .logo {
      font-size: 2.4rem;
      font-weight: 700;
      letter-spacing: 1px;
      color: var(--accent);
      margin-bottom: 12px;
      text-shadow: 0 0 10px rgba(0, 255, 204, 0.4);
    }

    .subtitle {
      font-size: 1.1rem;
      opacity: 0.9;
      margin-bottom: 26px;
    }

    .status {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 999px;
      background: rgba(0, 255, 204, 0.15);
      color: var(--accent);
      font-size: 0.9rem;
      margin-bottom: 28px;
    }

    .loader {
      margin: 0 auto 28px;
      width: 64px;
      height: 64px;
      border-radius: 50%;
      border: 4px solid rgba(255, 255, 255, 0.15);
      border-top-color: var(--accent);
      animation: spin 1.1s linear infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    .footer {
      font-size: 0.85rem;
      opacity: 0.7;
      margin-top: 10px;
    }
  </style>
</head>
<body>

  <div class="card">
    <div class="logo">Zane Gamez</div>
    <div class="subtitle">Unblocked Games site | Coming Soon</div>

    <div class="status">0% Complete</div>

    <div class="loader"></div>

    <div class="footer">
      ERRO LOADING:<br>
      404: Not Complete
    </div>
  </div>

</body>
</html>
